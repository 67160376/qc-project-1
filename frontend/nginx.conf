server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: proxy API if needed (commented)
  # location /api/ {
  #   proxy_pass http://api:4000/api/;
  # }
}
